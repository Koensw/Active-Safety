This should contain a description of the sensors on the model and where 
they are located

At start this should just be a function initing the sonar but later we 
should probably read a sensor model elsewhere.
